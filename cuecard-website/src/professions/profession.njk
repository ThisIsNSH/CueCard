---
pagination:
  data: professions
  size: 1
  alias: profession
permalink: "{{ profession.slug }}/index.html"
eleventyComputed:
  title: "{{ profession.title }}"
  description: "{{ profession.description }}"
  keywords: "{{ profession.keywords }}"
  canonical: "{{ profession.canonical }}"
  ogTitle: "{{ profession.ogTitle }}"
  ogDescription: "{{ profession.ogDescription }}"
  ogUrl: "{{ profession.canonical }}"
  ogImageWidth: "1280"
  ogImageHeight: "640"
  ogLocale: "en_US"
layout: layouts/profession.njk
---
